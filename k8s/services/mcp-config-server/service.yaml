apiVersion: v1
kind: Service
metadata:
  name: mcp-config-server
  labels:
    app: mcp-config-server
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
  selector:
    app: mcp-config-server

