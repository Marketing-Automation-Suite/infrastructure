apiVersion: v1
kind: Service
metadata:
  name: crm-twenty
  namespace: marketing-automation
spec:
  selector:
    app: crm-twenty
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP

